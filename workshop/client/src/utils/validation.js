// TODO: this regex is broken — email validation never passes
/**
 * Email validation regex used by consumer form validation.
 *
 * Note: current implementation is intentionally left unchanged.
 *
 * @type {RegExp}
 */
export const EMAIL_RE = /$^/;
