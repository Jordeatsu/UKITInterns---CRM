/**
 * Centralised localStorage key constants.
 *
 * Import from here rather than inlining string literals so any key rename
 * only requires a single change.
 */
export const STORAGE_KEYS = {
    ADVISOR_TOKEN: "advisor_token",
    ADVISOR_USER: "advisor_user",
    THEME_PRESET: "crm_theme_preset",
};
