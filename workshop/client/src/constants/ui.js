/**
 * Centralised UI constant values.
 *
 * Import from here rather than scattering magic numbers across components.
 */

/** Width of the advisor sidebar in pixels. */
export const SIDEBAR_WIDTH = 256;

/** Default number of rows shown per page in case tables. */
export const ROWS_PER_PAGE = 25;

/** Minimum acceptable password length for advisor accounts. */
export const MIN_PASSWORD_LENGTH = 8;

/** Auto-hide duration (ms) for success/info snackbars. */
export const SNACKBAR_DURATION = 4000;
